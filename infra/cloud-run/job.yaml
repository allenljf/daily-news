apiVersion: run.googleapis.com/v1
kind: Job
metadata:
  name: ${CLOUD_RUN_JOB_NAME}
spec:
  template:
    spec:
      template:
        spec:
          serviceAccountName: ${JOB_RUNTIME_SERVICE_ACCOUNT}
          maxRetries: 1
          timeoutSeconds: 900
          containers:
            - image: ${IMAGE_URI}
              command:
                - /app/daily-news-job
              resources:
                limits:
                  cpu: '1'
                  memory: 1Gi
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: DATABASE_URL
                      key: latest
